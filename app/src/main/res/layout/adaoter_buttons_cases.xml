<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center|top"
    android:layout_margin="15dp"
    android:gravity="center">

    <ImageView
        android:id="@+id/scerculerImageView"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_gravity="center"
        android:gravity="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ro" />

    <ImageView
        android:id="@+id/punchImageView"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_marginBottom="30dp"
        android:src="@drawable/vector_punch"
        app:layout_constraintBottom_toBottomOf="@id/scerculerImageView"
        app:layout_constraintEnd_toEndOf="@id/scerculerImageView"
        app:layout_constraintStart_toStartOf="@id/scerculerImageView"
        app:layout_constraintTop_toTopOf="@id/scerculerImageView" />

    <TextView
        android:id="@+id/textStateTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="5dp"
        android:gravity="center"
        android:text="@string/leave"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/scerculerImageView"
        app:layout_constraintStart_toStartOf="@id/scerculerImageView"
        app:layout_constraintTop_toBottomOf="@id/punchImageView" />


</androidx.constraintlayout.widget.ConstraintLayout>